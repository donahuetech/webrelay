(function() {
  module.exports = {
    setup: require('./setup'),
    "switch": require('./switch'),
    config: require('./config'),
    state: require('./state'),
    updateVariable: require('./updateVariable')
  };

}).call(this);
